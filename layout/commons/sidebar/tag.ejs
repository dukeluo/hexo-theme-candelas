<% if (site.tags.length){ %>
  <div class="widget-wrap">
    <h3 class="widget-title"><%= __('tag') %></h3>
    <div class="widget">
      <%- list_tags({
        show_count: true,
        amount: 42
      }) %>
    </div>
  </div>
<% } %>
