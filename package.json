{
  "name": "hexo-theme-candelas",
  "description": "next generation hexo theme of landscape",
  "private": true,
  "scripts": {
    "lint": "npx ejslint layout/*.ejs",
    "precommit": "npm run lint"
  },
  "devDependencies": {
    "ejs-lint": "^1.0.1",
    "pre-commit": "^1.2.2"
  }
}
